<%= form_for ([@post, @comment]) do |f| %>

  <div class="field">
    <%= f.label :Comments %><br />
    <%= f.text_area :text %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>

<% end %>